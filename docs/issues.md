# 项目问题备忘录

## 记录日期
2026-05-18

## 问题清单

### 1. 前端问题
- [ ] Vue 3 前端样式无法预览，界面"不堪入目"
- [ ] 前端构建后无样式或样式错误
- [ ] 需要检查 Ant Design Vue 样式导入是否正确

### 2. API 接口问题
- [ ] `/api/devices/scan` - 501 Unsupported method ('POST')
  - 前端发送 OPTIONS 预检请求，后端未处理
  - 需要添加 CORS 预检支持
- [ ] `/api/projects` - 501 Unsupported method ('OPTIONS')
  - 同上，所有 POST/PUT/DELETE 接口都有此问题
- [ ] `/api/keywords` - 404 File not found
  - 关键字未种子化到测试数据库
  - 集成测试使用独立 DB，无种子数据
- [ ] 所有接口定义请求方法错误
  - 前端 axios 发送请求方式与后端路由不匹配

### 3. 后端缺失功能
- [ ] 缺少输入验证和错误处理中间件
- [ ] 缺少 API 认证和授权
- [ ] 缺少数据库迁移（Alembic）
- [ ] 缺少日志配置
- [ ] 缺少 API 文档浏览（Swagger UI 不可访问）

### 4. 测试问题
- [ ] 集成测试使用独立数据库，关键字种子数据未同步
- [ ] 部分测试依赖外部状态

## 修复优先级

1. **P0 - 紧急**: CORS 预检请求处理、API 文档、关键字种子
2. **P1 - 重要**: 前端样式修复、输入验证中间件
3. **P2 - 一般**: 认证授权、数据库迁移、日志配置

## 修复计划

### Phase 1: 紧急修复
- 添加 CORS 预检处理
- 修复关键字种子在测试 DB 中的初始化
- 确保 Swagger UI 可访问

### Phase 2: API 修复
- 添加全局错误处理中间件
- 添加输入验证中间件
- 修复所有 API 路由方法匹配

### Phase 3: 前端修复
- 修复 Ant Design Vue 样式
- 确保前端能正确调用 API
- 添加错误处理

### Phase 4: 增强功能
- 添加认证授权
- 添加 Alembic 迁移
- 添加日志配置
